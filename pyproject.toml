[project]
name = "labs"
version = "0.1.0"
description = "Лабораторная работа"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.4.2",
    "pytest-asyncio>=0.24",
]
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]